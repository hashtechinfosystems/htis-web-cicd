apiVersion: apps/v1
kind: Deployment
metadata:
  name: htis-webapp
  namespace: htis-web-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: htis-webapp
  template:
    metadata:
      labels:
        app: htis-webapp
    spec:
      containers:
      - name: htis-webapp
        image: htcl/htis-web:v1.0.1
        ports:
        - containerPort: 80
